# Models

Store only approved model artefacts, manifests, and signatures required for
reproducible demonstrations. Do not commit private datasets, temporary
checkpoints, secrets, or unverified models.

Large artefacts should be managed through an approved release or artefact
storage process when they exceed normal Git limits.
