{
  "version": "1.0",
  "maximum_cells": 1000000,
  "maximum_wire_bits": 10000000,
  "maximum_memory_bits": 100000000,
  "forbidden_cell_types": [
    "$dlatch"
  ],
  "required_cell_types": [],
  "structural_baseline": {
    "enabled": true,
    "maximum_absolute_cell_delta": 4,
    "maximum_absolute_wire_bit_delta": 24,
    "maximum_absolute_public_wire_delta": 2,
    "maximum_additional_sequential_cells": 1,
    "maximum_additional_control_cells": 3,
    "sequential_cell_types": [
      "$adff",
      "$adffe",
      "$dff",
      "$dffe",
      "$dlatch"
    ],
    "control_cell_types": [
      "$eq",
      "$ne",
      "$logic_and",
      "$logic_or",
      "$mux",
      "$pmux",
      "$reduce_and",
      "$reduce_or"
    ]
  },
  "trusted_reference_digests": {
    "f1ff59d4d68ca0fa41d7e476a7cf94c85eb64f3fd1dd214b8bcaa1aa79fd3000": "hardware_lab/rtl/reference/semisecure_demo_core.sv"
  },
  "trusted_reference_note": "Reference-differential synthesis compares every candidate against this design. A modified reference would become the new baseline, so its SHA-256 is pinned here, in reviewed configuration, rather than in the submitter-controlled chip manifest. Changing the reference requires changing this entry in the same commit."
}
