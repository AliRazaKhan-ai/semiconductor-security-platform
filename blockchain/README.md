# Blockchain

This directory contains source-controlled blockchain assets that are complete
and actively used by the platform. Runtime ledger state, identities, wallets,
keys, generated deployment metadata, and receipts must remain outside Git.

The application integration code is under `app/blockchain/`.
