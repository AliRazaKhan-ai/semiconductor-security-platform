# Deployment

Use the root `Dockerfile`, `docker-compose.yml`, `gunicorn.conf.py`, and the
scripts under `scripts/deployment/` for supported deployments.

Empty legacy Kubernetes, nested Docker, reverse-proxy, systemd, and backup
placeholders were removed. Reintroduce them only with tested, executable
configuration.
